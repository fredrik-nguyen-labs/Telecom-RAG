# Only needed for the hosted/public Streamlit deployment.
# Never commit a real API key. Paste these values into Streamlit App settings -> Secrets.
OPENAI_API_KEY = "replace-with-project-scoped-key"
OPENAI_MODEL = "gpt-5.6-luna"
